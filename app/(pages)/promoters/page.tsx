"use client";

import React from "react";
import { Box } from "@chakra-ui/react";

interface Props {}

const Promoters = ({}: Props) => {
    return <Box>Promoters</Box>;
};

export default Promoters;
